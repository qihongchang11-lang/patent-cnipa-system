# Copy this file to `.env` and edit ONLY the values below.
# IMPORTANT: Do NOT commit `.env` (it contains secrets). `.gitignore` already ignores it.
#
# LLM_API_KEY:
# - Your provider API key (secret). Paste it into `.env` locally.
# - Example format shown below.
LLM_API_KEY=sk-c8b34b714d3e409e82b894362975caab
#
# LLM_BASE_URL:
# - OpenAI-compatible base URL for your provider.
LLM_BASE_URL=https://api.deepseek.com/v1
#
# LLM_MODEL:
# - Model name to use at the provider.
LLM_MODEL=deepseek-chat
